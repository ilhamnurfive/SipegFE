import {
  API_LOCAL
} from '@/config/axios'

import setParams from '@/utils/setParams'

export default {
  /* patch_usul_kgb(noUsul, data) {
    console.log(noUsul);
    return API_LOCAL().patch('/usul-kgb/' + noUsul, data)
  },
  patch_proses_kgb(id, data) {
    return API_LOCAL().patch('/detail-usul-kgb/' + id, data)
  } */
}